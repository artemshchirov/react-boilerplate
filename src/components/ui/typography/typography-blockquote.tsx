export function TypographyBlockquote() {
  return (
    <blockquote className="pl-6 mt-6 italic border-l-2">
      The blockquote element represents a section that is quoted from another
      source. Content inside a blockquote must be quoted from another source,
      whose address, if it has one, may be cited in the cite attribute. If the
      cite attribute is present, it must be a valid URL potentially surrounded
      by spaces.
    </blockquote>
  );
}
